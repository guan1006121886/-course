title: 斯蒂芬森的
description: 所发生的放松放松是
template:
  repo: '-template'
  name: ''
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
